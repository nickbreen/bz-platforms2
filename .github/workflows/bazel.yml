name: Bazel CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: gcr.io/bazel-public/bazel:latest

    steps:
    - uses: actions/checkout@v3
    - name: Bazel Build hellos
      run: bazel build //:hellos
